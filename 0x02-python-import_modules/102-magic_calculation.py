#!/bin/usr/bin/ptyhon3

def magic_calculation(a, b):
    from magic_calculation_102 import add, sub
    if (a < b):
        c = add(a, b)
        for p in range(4, 6):
            y = add(y, p)
        return(y)
    return(sub(a, b))
